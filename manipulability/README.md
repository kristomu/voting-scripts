Some Monte-Carlo code for determining the manipulability of three-candidate
IRV and worst-case manipulability of anything that elects from the resistant
set.

I haven't verified the latter, and may have missed some scenarios where the
method is indeed manipulable, since the result (0.16887 for 1e18 voters) is
incredibly close to IRV's. If correct, it would mean that three-candidate
IRV gets all its strategy resistance from the resistant set in this
particular instance.
